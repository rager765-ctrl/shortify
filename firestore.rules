rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Public read/write permissions for all collections (since login layer is bypassed)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
